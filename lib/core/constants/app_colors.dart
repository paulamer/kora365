import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0D0D0D);
  static const Color surface = Color(0xFF1A1A2E);
  static const Color surfaceVariant = Color(0xFF16213E);
  static const Color cardBg = Color(0xFF1E1E30);

  // Brand / Accent
  static const Color accent = Color(0xFF00D4AA);
  static const Color accentDark = Color(0xFF00A882);
  static const Color accentGlow = Color(0x3300D4AA);

  // Status
  static const Color live = Color(0xFFFF3B3B);
  static const Color liveGlow = Color(0x33FF3B3B);
  static const Color win = Color(0xFF4CAF50);
  static const Color draw = Color(0xFFFFB300);
  static const Color loss = Color(0xFFEF5350);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9E9E9E);
  static const Color textMuted = Color(0xFF616161);

  // Standings highlights
  static const Color championsLeague = Color(0xFF1565C0);
  static const Color europaLeague = Color(0xFFE65100);
  static const Color relegation = Color(0xFFB71C1C);

  // Divider / Border
  static const Color divider = Color(0xFF2A2A3E);
  static const Color border = Color(0xFF2E2E45);

  // Bottom nav
  static const Color bottomNavBg = Color(0xFF111120);
  static const Color bottomNavSelected = accent;
  static const Color bottomNavUnselected = Color(0xFF616161);
}
